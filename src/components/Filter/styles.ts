import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    continer:{
        flexDirection: "row",
        alignItems: "center",
        gap: 5

    },
    title:{
        fontSize: 16,
        fontWeight: 600,
    }
})